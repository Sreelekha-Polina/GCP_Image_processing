# GCP_Image_processing
Created  Cloud-function in Python where it gets executed when you upload image/Photo into GCS bucket gets resized by creating a thumbnail picture and will be uploaded back to GCS
